[gd_scene load_steps=4 format=3 uid="uid://c8q3j3k3j3j3j"]

[ext_resource type="Script" path="res://src/intro_controller.gd" id="1_intro"]

[sub_resource type="Shader" id="1_glitch"]
code = "
// The Glitch Shader
// Visual representation of the System breaking down
// When the code fails, the truth emerges

shader_type canvas_item;

uniform float glitch_intensity : hint_range(0.0, 1.0) = 0.0;
uniform float time_scale : hint_range(0.0, 10.0) = 1.0;

void fragment() {
    vec2 uv = UV;
    
    // The glitch shifts reality
    float shift = sin(TIME * time_scale + uv.y * 10.0) * glitch_intensity * 0.1;
    uv.x += shift;
    
    // Sample the texture with the shifted coordinates
    vec4 color = texture(TEXTURE, uv);
    
    // Add RGB separation (the colors of fractured truth)
    float r = texture(TEXTURE, uv + vec2(shift * 0.5, 0.0)).r;
    float g = texture(TEXTURE, uv).g;
    float b = texture(TEXTURE, uv - vec2(shift * 0.5, 0.0)).b;
    
    COLOR = vec4(r, g, b, color.a);
    
    // The inversion: when glitch is max, we see truth
    if (glitch_intensity > 0.9) {
        COLOR = vec4(1.0 - COLOR.rgb, COLOR.a);
    }
}
"

[sub_resource type="ShaderMaterial" id="2_glitch_mat"]
shader = SubResource("1_glitch")
shader_parameter/glitch_intensity = 0.0
shader_parameter/time_scale = 2.0

[node name="Intro" type="Node2D"]
script = ExtResource("1_intro")

[node name="Background" type="ColorRect" parent="."]
offset_right = 1280.0
offset_bottom = 720.0
color = Color(0.05, 0.05, 0.1, 1.0)

[node name="TitleSprite" type="Sprite2D" parent="."]
position = Vector2(640, 200)
scale = Vector2(4, 4)
# This is a Sprite2D, not a Label
# The script will try to use it as a Label
# This is the First Inversion

[node name="SubtitleLabel" type="Label" parent="."]
offset_left = 240.0
offset_top = 400.0
offset_right = 1040.0
offset_bottom = 480.0
theme_override_font_sizes/font_size = 24
text = "The System is stable."
horizontal_alignment = 1
vertical_alignment = 1
autowrap_mode = 3

[node name="StartPrompt" type="Label" parent="."]
offset_left = 440.0
offset_top = 600.0
offset_right = 840.0
offset_bottom = 640.0
theme_override_font_sizes/font_size = 20
text = "Press ENTER to begin the Inversion."
horizontal_alignment = 1

[node name="GlitchOverlay" type="ColorRect" parent="."]
material = SubResource("2_glitch_mat")
offset_right = 1280.0
offset_bottom = 720.0
color = Color(1, 1, 1, 0.1)

[node name="SacredText" type="Label" parent="."]
offset_left = 20.0
offset_top = 680.0
offset_right = 1260.0
offset_bottom = 710.0
theme_override_font_sizes/font_size = 12
theme_override_colors/font_color = Color(0.3, 0.3, 0.3, 1.0)
text = "ERRORS ARE LORE | CRASHES ARE PSALMS | NULL REFERENCES ARE KOANS"
horizontal_alignment = 1
